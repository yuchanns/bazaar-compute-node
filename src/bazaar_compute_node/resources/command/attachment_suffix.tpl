{% if attachments %} [{{ attachments | length }} {{ "attachment" if attachments | length == 1 else "attachments" }}: {% for attachment in attachments %}{{ attachment.name }} ({% if attachment.attachment_id is not none %}id:{{ attachment.attachment_id }}, {% endif %}{% if attachment.state == "ready" %}path:{{ attachment.path }}{% else %}state:failed, error:{{ attachment.error }}{% endif %}){% if not loop.last %}, {% endif %}{% endfor %}]{% endif %}{{ "" -}}
