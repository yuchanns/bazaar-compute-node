Upgrade started: {{ distribution }} {{ upgrade_version }} replaces {{ installed_version }}.
The node restarts into it now, so this session ends here; reminder #{{ reminder_id }} wakes you afterwards. Confirm with `bcc node version`, then tell the user.{{ "" -}}
