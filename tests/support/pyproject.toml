[project]
name = "bcn-test-support"
version = "0.1.0"
description = "Test-only adapters for Bazaar Compute Node"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
